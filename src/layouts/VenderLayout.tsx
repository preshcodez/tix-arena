// Commented out: renamed to VendorLayout.tsx (fixed typo, matches vendorComponents/pages/vendor naming).
// Kept here (rather than deleted) per project convention.
/*
import React from "react";
import VendorNav from "../components/vendorComponents/VendorNav";
import { Outlet } from "react-router-dom";

const VenderLayout = () => {
  return (
    <div>
      <VendorNav />
      <Outlet />
    </div>
  );
};

export default VenderLayout;
*/

// Commented out: unused stub — the live /signup route renders pages/auth/SignUp.tsx instead.
// Kept here (rather than deleted) per project convention.
/*
import React from "react";

const SignUpPage: React.FC = () => {
  return <div className="h-screen bg-[#0B0B0B] flex  "></div>;
};

export default SignUpPage;
*/

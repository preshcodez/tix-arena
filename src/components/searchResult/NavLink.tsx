// Commented out: was only used by searchResult/Navbar.tsx (also commented out).
// Kept here (rather than deleted) at the user's request. Note: this file imports
// "./SearchInput" and "./Logo", which were deleted before their contents were read
// and could not be restored (see chat for details) — do not uncomment as-is.
/*
import { Link } from "react-router-dom";
import SearchInput from "./SearchInput";
import Logo from "./Logo";

const NavLink = () => {
  return (
    <div className="flex items-center justify-center gap-8">
      <div className="flex items-center gap-8">
        <div className="flex items-center gap-8">
          <Link to="/explore" className="text-[16px] text-[#FFFFFF]">
            Explore
          </Link>
          <Link to="cinema" className="text-[16px] text-[#FFFFFF]">
            Cinema
          </Link>
        </div>
        <div>
          <SearchInput />
        </div>
      </div>
      <div>
        <Logo />
      </div>
    </div>
  );
};

export default NavLink;
*/
